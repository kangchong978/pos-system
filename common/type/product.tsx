interface Product {
    id?: number;
    name: string;
    image: string;
    category: string;
    description: string;
    code: string;
    price: number;
}

interface MyFile {
    file?: File;
    url?: string;
}